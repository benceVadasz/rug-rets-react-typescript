import styled from "@emotion/styled";

export const Container = styled.div({
    display: 'flex',
    height: '100vh',
    maxWidth: 1300,
    marginLeft: 'auto',
    marginRight: 'auto',
    padding: '0 10px',
})